name: "Required workflow not"
on:
  pull_request:
    paths-ignore:
      - 'llvm/**'

permissions:
  contents: read

jobs:
  required-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Require
        run: true

# Add a comment
# Add a comment
# Add a comment
